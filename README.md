# jquery.ajaxFileUpload

This is a fork version.

### Examples

```
$.ajaxFileUpload({
    url: '/',
    secureuri: false,
    fileElementId: file_id,
    dataType: 'json',
    success: function(response, status) {
    }
});
```